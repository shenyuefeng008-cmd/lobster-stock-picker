# L2 催化剂层落地完成

## 时间
2026-05-24 23:00

## 目标
将催化剂评分（L2 层）真正接入 `lobster_premarket_engine.py` 的全部 4 个选股函数，使选股结果受催化剂等级影响，并写入输出 JSON。

## 执行过程

### 问题诊断
1. **`catalyst_scoring` import 失败** — `scripts/` 目录不在 `sys.path`，导致 `CATALYST_AVAILABLE = False`，催化剂评分被静默跳过
2. **排序覆盖 bug（两处）** — `select_10_first_to_second()` 和 `select_10_divergence()` 中，催化剂排序（if 块内）完成后，函数级的原评分排序直接覆盖结果
3. **`select_20_sector()` 和 `select_30_trend()` 完全无催化剂评分** — 只有 1.0 两个函数有条件集成

### 修复步骤

**修复 1：`sys.path` 设置（L18-24）**
```python
# 确保scripts/目录在sys.path，使catalyst_scoring可被import
SCRIPT_DIR = Path(__file__).parent
if str(SCRIPT_DIR) not in sys.path:
    sys.path.insert(0, str(SCRIPT_DIR))
```
验证：`CATALYST_AVAILABLE = True` ✅

**修复 2：排序覆盖 bug（L247、L303）**
- 在 `select_10_first_to_second()` 的 `if CATALYST_AVAILABLE:` 块后加入 `else:` 分支
- 原评分排序移到 `else:` 内，仅在无催化剂评分时执行
- `select_10_divergence()` 同理
- 使用 Python 脚本按行号精确插入，绕过字符串匹配问题

**修复 3：`select_20_sector()` 加入催化剂评分（L354-366）**
- 在 `for s in stocks[:2]:` 循环内加入 `calculate_catalyst_score(sector)`
- 排序逻辑改为：有催化剂按 grade 排序，无则按原评分

**修复 4：`select_30_trend()` 加入催化剂评分（L433-445）**
- 从 `c.get('logic', '').split('/')[0]` 取赛道名
- 调用 `calculate_catalyst_score(track)` 获取评分
- 排序逻辑同上

**修复 5：输出 JSON 加入催化剂字段（L520-544）**
- 四个维度的输出字典均加入 `'催化剂': s.get('catalyst_grade', '?')` 和 `'建议': s.get('catalyst_action', '')`

## 验证

| 检查项 | 结果 |
|--------|------|
| `CATALYST_AVAILABLE` | `True` ✅ |
| 语法检查 (`python3 -m py_compile`) | 通过 ✅ |
| `else:` 分支（L247、L305） | 已正确插入 ✅ |
| 4 个函数均有催化剂评分 | grep 确认 ✅ |
| 输出 JSON 含催化剂字段 | L520-544 ✅ |

## 文件变更

| 文件 | 操作 | 说明 |
|------|------|------|
| `scripts/lobster_premarket_engine.py` | 编辑 | +5 处修改，当前 551 行 |

## 待验证

1. **下周一 07:00 盘前任务** — 确认运行时催化剂评分正常输出（需真实市场数据）
2. **仓位分配建议** — 当前仅评分/排序，未根据 Tier 输出 `recommended_position`（L2 框架要求 Tier1=20-25% 等）
3. **`select_30_trend()` 的赛道名映射** — `logic` 字段格式为 `"赛道/状态"`，映射可能不准确

## 关键决策

1. **不修改 `catalyst_scoring.py`** — 该模块接口已完整，问题在调用侧
2. **排序策略** — 催化剂等级优先（S>A>B>C>D），同级按原评分，无催化剂时按原评分
3. **不实现自动仓位分配** — L2 框架的 Tier→仓位映射留给后续迭代，当前仅提供评分/建议

---
