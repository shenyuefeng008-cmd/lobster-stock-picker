# 龙虾卖点监控任务 Artifact
## 任务目标
执行龙虾卖点监控定时任务，按照 `CRON_SELLPOINT_TASK.md` 步骤1-6执行，完成持仓读取、实时行情获取、分时追踪、止损止盈判断、自动卖出、情绪强制减仓全流程，输出监控结果。

## 关键推理
1. **持仓状态**：`trading/holdings.json` 文件不存在，说明当前无持仓，止损止盈判断、自动卖出步骤无执行对象，直接跳过。
2. **情绪数据获取**：多次尝试通过 akshare、乐咕乐股接口获取涨跌家数均失败（连接中断/接口无响应），无法判断当前情绪区间，情绪强制减仓步骤无法执行。
3. **流程文件缺失**：`CRON_SELLPOINT_TASK.md` 文件不存在，无法按照约定的步骤1-6规范执行监控流程，仅能基于现有配置输出基础结果。

## 结论
- 无持仓，无卖点触发，无需执行任何卖出/减仓操作。
- 需补充 `CRON_SELLPOINT_TASK.md` 文件和持仓文件，以完善监控流程、实现全流程自动化执行。
- 建议排查涨跌家数接口连通性，确保情绪判断模块正常运行。