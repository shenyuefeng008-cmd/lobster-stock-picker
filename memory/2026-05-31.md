## IMA彻底修复 (23:35)

### 用户指令: "立即执行" IMA 修复方案

### 修复3步走

**1. ima_sync.sh — 去掉 set -e + 加错误日志**
- 删除 `set -e`（任何非0返回不再中断脚本）
- 新增 `log_error()` / `log_info()` 函数，写入 `/tmp/ima-errors.log`
- 所有 `import_doc`/`add_knowledge`/`校验` 失败点都加 `log_error` 调用

**2. 所有 CRON_MD — 错误不再静默吞掉**
修改了9个文件，把 `2>/dev/null` 或 无redirect 统一改为 `2>>/tmp/ima-errors.log`：
| 文件 | 修复 |
|------|------|
| CRON_BID_AUTO_BUY.md | +`2>>/tmp/ima-errors.log` |
| CRON_CLOSING_TASK.md | +`2>>/tmp/ima-errors.log` |
| CRON_MIDDAY_TASK.md | +`2>>/tmp/ima-errors.log` |
| CRON_PREMARKET_TASK.md | +`2>>/tmp/ima-errors.log` |
| CRON_INTRADAY_PATROL_TASK.md | +`2>>/tmp/ima-errors.log` |
| CRON_EVOLUTION_TASK.md | +`2>>/tmp/ima-errors.log` |
| CRON_BLOG_CLOSING_TASK.md | 改 `2>/dev/null` → `2>>/tmp/ima-errors.log` |
| CRON_BLOG_MIDDAY_TASK.md | 同上 |
| _DEPRECATED_CRON_LATE_SESSION_TASK.md | 同上 |

**3. daily_ima_sync.sh — 也去掉 set -e**
- `set -e` → `set +e`

### 效果
- ❌ 旧：任何非零返回 → 脚本当场死亡 + 错误被 `/dev/null` 吞掉
- ✅ 新：脚本继续执行 + 错误写入 `/tmp/ima-errors.log` 可查
- 排查命令：`tail -50 /tmp/ima-errors.log`