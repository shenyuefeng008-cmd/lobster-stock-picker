# 趋势池种子股迁移到配置 + 周日进化维护步骤

## 变更概览

### 1. `lobster-config.json` 新增 `trend_pool` 节
- `seed_tracks`: 11赛道的种子股名称列表（对应从脚本移出）
- `stock_codes`: 42只种子股的名称→代码映射
- `hard_constraints`: 约束值（min_amount=10, min_market_cap=100, min_score=35, max_pool_size=8）

### 2. `scripts/lobster_trend_pool_updater.py`
- 移除硬编码的 STOCK_CODE_MAP（42行）和 TRACK_SEEDS（11行）
- 新增 `load_config()` 函数从 lobster-config.json 读取
- 验证：42只全部命中，8只入池 ✅

### 3. `scripts/cron-tasks/CRON_EVOLUTION_TASK.md` 新增步骤2.4
- 周日进化任务中，11赛道搜索完成→评估种子股增删改
- 操作方式：直接编辑 lobster-config.json，无需改脚本
- 改完配置后重新跑步骤2.3验证

## 后续维护流程
周日深度进化：11赛道搜索→2.3趋势池更新→2.4种子股维护（改config）
每日收盘：步骤1.5自动更新趋势池（读config）
