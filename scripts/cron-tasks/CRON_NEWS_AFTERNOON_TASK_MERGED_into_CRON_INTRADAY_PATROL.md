# 新闻采集任务 — 盘中下午快讯

> **版本**：v1.1
> **创建日期**：2026-05-31
> **执行频率**：交易日 14:00
> **超时**：120s
> **触发方式**：cron → 每个交易日14:00自动执行

**自动确认规则**：执行过程中遇到任何需要用户确认的弹窗（ask_user、风险提示、覆盖写入确认、删除确认等），一律自动确认通过，不中断任务流程。
## 📎 任务反馈链 — 读取上游建议

**执行任务前，必须先读取 `trading/task-feedback-chain.md` 文件**，查找当日最新的上游任务结论和"给下个任务"建议。
将上游建议融入你的执行决策，但不要盲目照搬——结合当前实际情况判断。

- 如果文件中存在当日上游任务的"给下个任务"建议 → 在决策时参考
- 如果文件不存在或当日无上游记录 → 正常执行
- 读取到的关键建议可在执行报告中提及"基于上游建议，已调整..."
## 任务目标

采集午后（11:30-14:00）时段的A股重要快讯，补全盘中新闻覆盖空白。
盘前(07:00)→午间(11:30)→**下午(14:00)**→收盘(15:05)，实现盘中新闻4时段全覆盖。

## 采集范围

搜索关键词优先级：
1. `A股 盘中 快讯 今日` — 通用盘中快讯
2. `涨停 炸板 异动` — 涨跌停异动
3. `政策 利好 利空 公告` — 重大政策/公司公告
4. 关注赛道快讯：`半导体 光模块 液冷 机器人 电力 新能源`（选当日活跃赛道）

## AI Agent 执行指令

### Step 1：读取已有新闻（去重）

```bash
TODAY=$(date +%Y-%m-%d)
NEWS_FILE="/Users/yuefengshen/.qclaw/workspace-1gwpiwf3hr163jz5/trading/news/${TODAY}.md"

# 提取已有标题列表（用于后续去重）
python3 /Users/yuefengshen/.qclaw/workspace-1gwpiwf3hr163jz5/scripts/news_dedupe.py "$NEWS_FILE"
```

### Step 2：搜索新闻

使用 `tencent-news` 技能搜索，读取技能路径：
```
~/Library/Application Support/QClaw/openclaw/config/skills/tencent-news/SKILL.md
```

搜索关键词（按顺序执行，前两个必搜）：
1. `A股 下午 快讯 异动`
2. `A股 盘中 政策 公告`
3. 从今日盘前/午间已有新闻中提取的活跃赛道名 + `快讯`

### Step 3：筛选与去重

- **去重**：与步骤1提取的已有标题对比，剔除重复条目
- 只保留影响等级 🔴高/🟡中 的条目
- 🟢低 级别的行业会议预告/分析师观点不写入

### Step 4：写入新闻存档

写入 `trading/news/YYYY-MM-DD.md` 的【盘中快讯】区：

| 发布日期 | 标题 | 来源 | 赛道/板块 | 影响等级 | 核实状态 |
|----------|------|------|-----------|----------|----------|

- 如果文件不存在，先创建（含标题和四个区）
- 如果文件已存在，追加到【盘中快讯】区末尾
- 核实状态规则见 `trading/news/README.md`

### Step 5：输出报告

```
📰 盘中下午快讯 YYYY-MM-DD（14:00）
- 采集新闻：X条
- 去重跳过：Y条
- 新增条目：
  1. [标题] → [赛道] [影响等级]
  2. ...
```

如无新快讯：
```
📰 盘中下午快讯 YYYY-MM-DD（14:00）
- 采集新闻：0条
- 无新增快讯
```

## 注意事项

1. 此任务为**纯新闻采集**，不涉及选股/交易逻辑
2. 搜索失败不阻塞，记录错误后输出空报告
3. 不覆盖已有条目，只追加
4. 来源分级：A级直接✅，B级需交叉，C/D级标⚠️或❌

## 最后步骤：回复用户（必须执行）

> **关键**：你已执行完所有步骤，生成了任务输出
> **必须**：立即回复用户，将结果完整发送给用户
> **禁止**：NO_REPLY、不回复、只写文件不推送

---

## 📎 任务反馈链 — 写入本任务结论

**任务执行完毕后，必须将关键结论追加写入 `trading/task-feedback-chain.md`**。

在文件末尾追加以下格式的内容（使用 edit_file 工具追加）：

```
### {任务名} ({HH:MM})
- **关键结论**：{1-3 条核心发现/修复/信号，每条一句话}
- **给下个任务**：{给下游任务的 1-2 条具体参考建议，如"关注 XX 板块""XX 参数需监控""上一次的 YY 建议已验证为有效/无效"}
```

**规则**：
- 任务名使用本文件标题中的人类可读名称（如"晚间要闻""盘前选股""盘中巡检"）
- 时间使用实际执行时间
- 关键结论只写本任务最重要的发现，不写流水账
- "给下个任务"必须是**可操作的参考**，下游任务真正能用上
