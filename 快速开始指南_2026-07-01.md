# 快速开始指南 - 龙虾超短交易系统

**接收人**: [待填写]  
**开始日期**: 2026-__-__

---

## 📋 第一天（熟悉系统）

### 上午（2小时）
- [ ] 阅读 `【交接包】龙虾超短交易系统_2026-07-01.md` 了解系统架构
- [ ] 阅读 `MEMORY.md` 了解系统历史和已知问题
- [ ] 阅读 `AGENTS.md` 了解工作区规则
- [ ] 验证Python环境：`python3 --version` + `pip3 list | grep akshare`

### 下午（2小时）
- [ ] 验证Cron任务：`openclaw cron list --enabled | grep "龙虾"` （应返回20个）
- [ ] 手动运行盘中巡检（测试）：
  ```bash
  cd scripts
  python3 lobster_intraday_patrol.py 2>&1 | head -100
  ```
- [ ] 查看当前持仓：
  ```bash
  cd scripts
  python3 -c "from simulated_trading import status; print(status())"
  ```

### 晚上（1小时）
- [ ] 决定是否需要继续自动交易
  - **继续**: 确保所有Cron任务已启用
  - **暂停**: 执行 `openclaw cron list | grep 龙虾 | awk '{print $1}' | xargs -I {} openclaw cron update {} --enabled false`
- [ ] 阅读 `trading/BUG_LOG.md` 了解历史Bug

---

## 📋 第二天（深入代码）

### 上午（3小时）
- [ ] 阅读 `scripts/simulated_trading.py` 了解模拟交易核心逻辑
- [ ] 阅读 `scripts/lobster_intraday_patrol.py` 了解盘中巡检逻辑
- [ ] 阅读 `scripts/lobster_premarket_engine.py` 了解盘前选股逻辑
- [ ] 修复P0问题（见交接文档）

### 下午（2小时）
- [ ] 阅读 `config/lobster-config.json` 了解系统参数
- [ ] 手动修改一个参数（如 `stop_loss`），验证系统响应
- [ ] 阅读 `scripts/cron-tasks/CRON_DAILY_EVOLUTION_TASK.md` 了解每日进化逻辑

---

## 📋 第三天（实战测试）

### 上午（交易时间）
- [ ] 观察盘前选股（07:00）: 查看 `/tmp/lobster_premarket_candidates.json`
- [ ] 观察竞价买入（09:30）: 查看 `trading/模拟持仓.json` 是否新增持仓
- [ ] 观察盘中巡检（10:00-14:30）: 查看推送消息

### 下午（收盘后）
- [ ] 查看收盘复盘报告：检查 `trading/reports/` 目录
- [ ] 查看IMA知识库：确认复盘已归档
- [ ] 记录问题：将遇到的问题写入 `memory/2026-MM-DD.md`

---

## 🚨 紧急情况处理

### 场景1：需要立即停止所有交易
```bash
# 方法1：禁用所有Cron任务
openclaw cron list --enabled | grep "龙虾" | awk '{print $1}' | while read id; do
  openclaw cron update $id --enabled false
done

# 方法2：卖出所有持仓（谨慎！）
cd /Users/yuefengshen/.qclaw/workspace-1gwpiwf3hr163jz5/scripts
python3 -c "
from simulated_trading import sell_all
result = sell_all(reason='紧急止损')
print(result)
"
```

### 场景2：持仓数据异常
```bash
# 1. 备份当前持仓
cp trading/模拟持仓.json trading/模拟持仓.json.backup_$(date +%Y%m%d_%H%M%S)

# 2. 手动修复（参考MEMORY.md中的修复记录）
# 3. 验证修复
cd scripts && python3 -c "from simulated_trading import validate_capital; validate_capital()"
```

### 场景3：Cron任务失败
```bash
# 查看最近失败的Cron
openclaw cron list --include-disabled | grep -A 5 "lastRunStatus.*error"

# 手动运行失败的Cron任务
openclaw cron run [cron_id]
```

---

## 📞 联系信息

- **交接人**: Yuefeng (微信/电话：[待填写])
- **紧急联系**: [待填写]

---

## ✅ 第三天结束时检查清单

- [ ] 系统运行正常（无P0/P1错误）
- [ ] 理解核心模块逻辑（模拟交易/盘中巡检/盘前选股）
- [ ] 成功修复至少1个已知问题
- [ ] 观察过完整的交易流程（盘前→竞价→盘中→收盘）
- [ ] 记录至少3条改进建议（写入 `memory/2026-MM-DD.md`）

---

**第三天结束签名**: ________  
**交接完成日期**: 2026-__-__
