# 系统架构清理完成

## 已完成的改动

### 废弃文件归档（trading/_废弃/）
| 文件 | 替代方案 |
|------|----------|
| `trading-state.json` → 归档 | 新 `trading/系统状态.json` |
| `sentiment_cache.json` → 归档 | blog_auto_writer实时查询+新浪备用 |

### 脚本修改

| 脚本 | 改动 |
|------|------|
| `lobster_premarket_engine.py:404` | 改读 `系统状态.json` 的 `yesterday.up_count` 替代废弃的 state.json |
| `blog_auto_writer.py` | 移除 `save_sentiment_cache()`/`load_sentiment_cache()`，legulegu失败后走新浪分页统计 |
| `CRON_CLOSING_TASK.md` | 读/写 `系统状态.json`（读取维度 + 收盘后写入今日数据） |
| `CRON_DAILY_EVOLUTION_TASK.md` | 审计清单新增 `系统状态.json`，旧state.json标记废弃 |

### 数据流最终形态

```
收盘复盘(15:05) → 写入 trading/系统状态.json
                  ├── today.up_count  (收盘情绪)
                  ├── yesterday.up_count (滚动)
                  └── today.dimension (情绪维度)

盘前引擎(07:00) → 读 系统状态.json → yesterday_up
                  判断"连续2日>2500"激活3.0

博客(12:00/16:00) → fetch_live_emotion (legulegu→新浪)
                    不缓存旧数据

买点监控(每30min) → fetch_live_emotion (legulegu→新浪)
                    拒绝旧数据fallback
```
