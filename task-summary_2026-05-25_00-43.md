# L3 参数外置收尾 — 2026-05-25 00:43

## 目标

把 `scoring_calculator.py`、`lobster_premarket_engine.py`、`lobster_bid_filter_v2.py` 中的硬编码阈值全部迁移到 `lobster-config.json`，实现参数可配置、可自我进化。

## 完成工作

### 1. `lobster-config.json` 升级至 v1.3

**新增字段：**

| 字段 | 用途 | 来源 |
|------|------|------|
| `scoring_models` | 四维度打分模型（权重/区间/入选阈值） | 原 `scoring_calculator.py` 的 `SCORING_MODELS` 字典 |
| `bid_filter_thresholds` | 四维度竞价过滤条件（涨跌幅/竞量） | 原 `lobster_bid_filter_v2.py` 的4个 `elif` 硬编码 |

**版本记录：**
- v1.1（初始）：emotion、1.0/2.0/3.0 各维度参数、catalyst、stop_loss
- v1.2（本次）：新增 `scoring_models`（打分模型参数化）
- v1.3（本次）：新增 `bid_filter_thresholds`（竞价过滤参数化）

### 2. `scoring_calculator.py` 改造

**修改内容：**
- 在 `def calculate_score(` 前插入 config 加载代码
- 优先读 `lobster-config.json -> scoring_models`
- 读取失败时 fallback 到脚本内的 hardcoded `SCORING_MODELS`

**验证：** `python3 -m py_compile` 通过 ✅

### 3. `lobster_bid_filter_v2.py` 改造

**修改内容：**
- 头部新增 `BID_THRESHOLDS` 全局变量，从 `lobster-config.json -> bid_filter_thresholds` 读取
- 4个 `elif` 条件块改为读 `BID_THRESHOLDS[维度名][min_change_pct/max_change_pct/min_volume]`
- `2.0板块卡位` 的 `max_change_pct` 支持 `None`（无上限）

**替换明细：**

| 维度 | 原硬编码 | 现读 config 字段 |
|------|----------|------------------|
| 1.0一进二 | `6 <= change_pct <= 10 and volume >= 1500` | `min_change_pct=6, max_change_pct=10, min_volume=1500` |
| 1.0分歧低吸 | `-3 <= change_pct <= 0` | `min_change_pct=-3, max_change_pct=0` |
| 2.0板块卡位 | `change_pct > 5 and volume >= 1000` | `min_change_pct=5, max_change_pct=None, min_volume=1000` |
| 3.0趋势低吸 | `-2 <= change_pct <= 3` | `min_change_pct=-2, max_change_pct=3` |

**验证：** `python3 -m py_compile` 通过 ✅

### 4. `lobster_premarket_engine.py` 改造

**修改内容：**

| 行号 | 原硬编码 | 改造后 |
|--------|----------|--------|
| L266 | `if lb < 2 or lb >= 4:` | `if lb < _min_lb or lb > _max_lb:`（读 `1.0_divergence.min_lb/max_lb`） |
| L228-232 | `sc >= 3: +15 / sc >= 2: +10 / sc >= 1: +5` | 读 `1.0_first_to_second.score_sector.ge_3/ge_2/ge_1` |
| L420-422 | `amount >= 10: +15 / amount >= 5: +10` | 读 `3.0_trend.score_amount_ge_10/ge_5` |

**验证：** `python3 -m py_compile` 通过 ✅

## 系统现状（2026-05-25 00:43）

### 全系统闭环进度

| 模块 | 状态 | 说明 |
|------|------|------|
| L4 P0 最终否决权 | ✅ 完成 | `final_veto_check()` 五道否决条件 |
| L4 P1 错误反馈闭环 | ✅ 完成 | `generate_error_report()` 按维度胜率分析 |
| L2 催化剂闭环 | ✅ 完成 | `update_catalyst_verification()` 接入卖出流程 |
| L3 参数外置 | ✅ 完成（本次） | `scoring_models` + `bid_filter_thresholds` 全部入 config |
| L1 市场环境参数外置 | ❌ 未完成 | 情绪阈值在 `lobster-config.json` 已配置，但缺少反馈机制 |
| 进化任务参数驱动 | ❌ 未完成 | `CRON_DAILY_EVOLUTION_TASK.md` 目前只审计不优化参数 |

### 待办（下阶段）

1. **L1 反馈机制**：记录情绪预测准确率 → 驱动 `emotion` 阈值调整
2. **进化任务增强**：读取 `feedback.json` → 决策调整 `lobster-config.json` 参数
3. **盘前引擎/买点监控/卖点监控/收盘复盘闭环改造**：接入 `feedback.json` 反馈链路

## 关键文件

| 文件 | 绝对路径 | 状态 |
|------|----------|------|
| lobster-config.json | `/Users/yuefengshen/.qclaw/workspace-1gwpiwf3hr163jz5/lobster-config.json` | ✅ v1.3 |
| scoring_calculator.py | `/Users/yuefengshen/.qclaw/workspace-1gwpiwf3hr163jz5/scripts/scoring_calculator.py` | ✅ 改完 |
| lobster_bid_filter_v2.py | `/Users/yuefengshen/.qclaw/workspace-1gwpiwf3hr163jz5/scripts/lobster_bid_filter_v2.py` | ✅ 改完 |
| lobster_premarket_engine.py | `/Users/yuefengshen/.qclaw/workspace-1gwpiwf3hr163jz5/scripts/lobster_premarket_engine.py` | ✅ 改完 |
| feedback.json | `/Users/yuefengshen/.qclaw/workspace-1gwpiwf3hr163jz5/trading/feedback.json` | ✅ 已有 |

## 结论

L3 参数外置已完成。所有硬编码阈值（打分模型、竞价过滤、连板范围、成交额阈值）全部迁移至 `lobster-config.json`，可通过每日进化任务自动调整。

下周一（2026-05-25）开盘前验证项：
- [ ] 07:00 盘前任务 `scoring_models` 是否正常加载
- [ ] 09:25 竞价过滤 `bid_filter_thresholds` 是否按 config 执行
- [ ] 买入/卖出时 `feedback.json` 是否正确记录

---
*2026-05-25 00:43 GMT+8 — L3 改造完成，距离开盘还有 6h17m*
