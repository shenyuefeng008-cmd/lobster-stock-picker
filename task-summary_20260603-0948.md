# 竞价兜底修复 | 2026-06-03 09:48

## 问题
连续两天（06-02、06-03）盘前选股cron任务（07:00）汇报"成功"但 `/tmp/lobster_premarket_candidates.json` 未生成：
- 06-02 09:25 cron：汇报"情绪高潮选出粤电力A"但无文件
- 06-03 09:25 cron：同样问题

后果：09:25竞价过滤cron读取文件失败 → `/tmp/lobster_bid_result.json` 不存在 → 自动买入跳过

## 根因
CRON_PREMARKET_TASK.md 中的脚本执行步骤以 `bash <<'EOF' ... EOF` heredoc 代码块形式嵌入 prompt，agent 读取理解但不会自动执行——它用自己的内部知识合成了摘要输出，绕过了真正的数据获取和文件写入。

## 修复
1. **CRON_PREMARKET_TASK.md → v7**：所有操作改为 agent 直接执行的步骤格式，用 exec 工具调用，不再用 bash 代码块
2. **CRON_BID_RECOVERY_TASK.md 新建（v1）**：09:20 兜底任务，前置检查文件是否存在，缺失则立即补救
3. **新增 cron「龙虾竞价兜底」**（id: 26f61540）：交易日 09:20 触发，读取 CRON_BID_RECOVERY_TASK.md 执行

## 今日补救
手动运行 `lobster_premarket_engine.py`（09:43），候选池文件已生成（17只候选股）。09:20 兜底任务从明天起生效。

## 结论
- 根因：bash heredoc 代码块 agent 不执行
- 修复：改为 exec 工具调用 + 09:20 保险丝 cron
